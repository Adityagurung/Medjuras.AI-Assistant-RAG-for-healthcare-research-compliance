apiVersion: 1

datasources:
  - name: Medjuras.AI-datasource
    type: postgres
    access: proxy
    url: postgres:5432
    database: ragdb
    user: 
    secureJsonData:
      password: 
    jsonData:
      sslmode: disable
      maxOpenConns: 100
      maxIdleConns: 100
      connMaxLifetime: 14400
    isDefault: true
